version: "3"
services:
  pod_health:
    container_name: pod_health
    image: registry.gitlab.com/panupong.dev.webb/gitops-application-builder:f9523ad
    environment:
      - PORT=3000
    ports:
      - "4600:3000"
    


networks:
  default:
    external:
      name: applications_dev
